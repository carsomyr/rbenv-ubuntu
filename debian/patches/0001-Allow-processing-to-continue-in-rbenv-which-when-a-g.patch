From: Roy Liu <carsomyr@gmail.com>
Date: Mon, 30 Jul 2012 20:38:01 -0400
Subject: Allow processing to continue in rbenv-which when a given command
 isn't found with the system Ruby

---
 libexec/rbenv-which | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/libexec/rbenv-which b/libexec/rbenv-which
index c4966c7..204988b 100755
--- a/libexec/rbenv-which
+++ b/libexec/rbenv-which
@@ -50,7 +50,7 @@ fi
 
 if [ "$RBENV_VERSION" = "system" ]; then
   PATH="$(remove_from_path "${RBENV_ROOT}/shims")"
-  RBENV_COMMAND_PATH="$(command -v "$RBENV_COMMAND")"
+  RBENV_COMMAND_PATH="$(command -v "$RBENV_COMMAND" || true)"
 else
   RBENV_COMMAND_PATH="${RBENV_ROOT}/versions/${RBENV_VERSION}/bin/${RBENV_COMMAND}"
 fi
-- 
